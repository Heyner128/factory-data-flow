spring:
  docker:
    compose:
      file: ../compose-dev.yaml
      host: ${DOCKER_HOST:localhost}
  config:
   import: optional:file:../.env[.properties]
  jpa:
   show-sql: true
   properties:
     hibernate:
       format_sql: true
  devtools:
   restart:
     enabled: true
  liquibase:
    contexts: demo